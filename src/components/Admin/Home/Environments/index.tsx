import { HomeSection } from "../HomeSection";
import * as s from "./styles";

export const Environments = () => {
  return (
    <HomeSection title={"Environments"}>
      <s.WidgetsContainer>
        {/* // TODO: Add blurred images */}
      </s.WidgetsContainer>
    </HomeSection>
  );
};
